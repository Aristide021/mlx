diff --git a/mlx/array.h b/mlx/array.h
index 60d5e50bb..55ce29b4a 100644
--- a/mlx/array.h
+++ b/mlx/array.h
@@ -19,7 +19,7 @@ namespace mlx::core {
 class Primitive;
 
 using Deleter = std::function<void(allocator::Buffer)>;
-using ShapeElem = int32_t;
+using ShapeElem = int64_t;
 using Shape = SmallVector<ShapeElem>;
 using Strides = SmallVector<int64_t>;
 
diff --git a/mlx/backend/common/utils.h b/mlx/backend/common/utils.h
index c6d782061..a358b6e04 100644
--- a/mlx/backend/common/utils.h
+++ b/mlx/backend/common/utils.h
@@ -14,17 +14,18 @@ namespace mlx::core {
 std::filesystem::path current_binary_dir();
 
 inline int64_t
-elem_to_loc(int elem, const Shape& shape, const Strides& strides) {
+elem_to_loc(int64_t elem, const Shape& shape, const Strides& strides) {
   int64_t loc = 0;
   for (int i = shape.size() - 1; i >= 0; --i) {
-    auto q_and_r = ldiv(elem, shape[i]);
-    loc += q_and_r.rem * strides[i];
-    elem = q_and_r.quot;
+    int64_t q = elem / shape[i];
+    int64_t r = elem % shape[i];
+    loc += r * strides[i];
+    elem = q;
   }
   return loc;
 }
 
-inline int64_t elem_to_loc(int elem, const array& a) {
+inline int64_t elem_to_loc(int64_t elem, const array& a) {
   if (a.flags().row_contiguous) {
     return elem;
   }
diff --git a/mlx/backend/cuda/compiled.cpp b/mlx/backend/cuda/compiled.cpp
index 402c8ce8b..b6efa5348 100644
--- a/mlx/backend/cuda/compiled.cpp
+++ b/mlx/backend/cuda/compiled.cpp
@@ -49,7 +49,7 @@ struct FusedKernelBuilder {
     }
     if (!contiguous) {
       params.push_back(
-          "const __grid_constant__ cuda::std::array<int32_t, NDIM> shape");
+          "const __grid_constant__ cuda::std::array<int64_t, NDIM> shape");
     }
     params.push_back("IdxT size");
 
diff --git a/mlx/backend/cuda/device/gather.cuh b/mlx/backend/cuda/device/gather.cuh
index 7dbd84ac3..7d9d56c54 100644
--- a/mlx/backend/cuda/device/gather.cuh
+++ b/mlx/backend/cuda/device/gather.cuh
@@ -21,7 +21,7 @@ __global__ void gather(
     uint32_t slice_size,
     const __grid_constant__ cuda::std::array<int32_t, NIDX> axes,
     const __grid_constant__ cuda::std::array<IdxT*, NIDX> indices,
-    const __grid_constant__ cuda::std::array<int32_t, NIDX * IDX_NDIM>
+    const __grid_constant__ cuda::std::array<int64_t, NIDX * IDX_NDIM>
         indices_shape,
     const __grid_constant__ cuda::std::array<int64_t, NIDX * IDX_NDIM>
         indices_strides) {
diff --git a/mlx/backend/cuda/device/indexing.cuh b/mlx/backend/cuda/device/indexing.cuh
index 31cba1a90..97bc6df63 100644
--- a/mlx/backend/cuda/device/indexing.cuh
+++ b/mlx/backend/cuda/device/indexing.cuh
@@ -19,11 +19,11 @@ index_to_dims(T index, T dim1, T dim2) {
 
 // Get absolute index from possible negative index.
 template <typename IdxT>
-inline __host__ __device__ auto absolute_index(IdxT idx, int32_t size) {
+inline __host__ __device__ auto absolute_index(IdxT idx, int64_t size) {
   if constexpr (cuda::std::is_unsigned_v<IdxT>) {
     return idx;
   } else {
-    return static_cast<int32_t>(idx < 0 ? idx + size : idx);
+    return static_cast<int64_t>(idx < 0 ? idx + size : idx);
   }
 }
 
diff --git a/mlx/backend/cuda/device/scatter.cuh b/mlx/backend/cuda/device/scatter.cuh
index c5378bfc4..a5a664f13 100644
--- a/mlx/backend/cuda/device/scatter.cuh
+++ b/mlx/backend/cuda/device/scatter.cuh
@@ -30,7 +30,7 @@ __global__ void scatter(
     int32_t out_ndim,
     const __grid_constant__ cuda::std::array<int32_t, NIDX> axes,
     const __grid_constant__ cuda::std::array<IdxT*, NIDX> indices,
-    const __grid_constant__ cuda::std::array<int32_t, NIDX * IDX_NDIM>
+    const __grid_constant__ cuda::std::array<int64_t, NIDX * IDX_NDIM>
         indices_shape,
     const __grid_constant__ cuda::std::array<int64_t, NIDX * IDX_NDIM>
         indices_strides) {
diff --git a/mlx/backend/cuda/device/utils.cuh b/mlx/backend/cuda/device/utils.cuh
index 2e017ba21..47593e0eb 100644
--- a/mlx/backend/cuda/device/utils.cuh
+++ b/mlx/backend/cuda/device/utils.cuh
@@ -25,7 +25,7 @@ namespace mlx::core::cu {
 
 // To pass shape/strides to kernels via constant memory, their size must be
 // known at compile time.
-using Shape = cuda::std::array<int32_t, MAX_NDIM>;
+using Shape = cuda::std::array<int64_t, MAX_NDIM>;
 using Strides = cuda::std::array<int64_t, MAX_NDIM>;
 
 // Vectorized load/store.
@@ -257,7 +257,7 @@ struct Limits<complex_t<T>> {
 
 template <typename IdxT = int64_t>
 inline __host__ __device__ IdxT
-elem_to_loc(IdxT elem, const int* shape, const int64_t* strides, int ndim) {
+elem_to_loc(IdxT elem, const int64_t* shape, const int64_t* strides, int ndim) {
   IdxT loc = 0;
   for (int i = ndim - 1; i >= 0 && elem > 0; --i) {
     loc += (elem % shape[i]) * IdxT(strides[i]);
@@ -269,7 +269,7 @@ elem_to_loc(IdxT elem, const int* shape, const int64_t* strides, int ndim) {
 // Optimize when the ndim is known at compile time.
 template <int NDIM, typename IdxT = int64_t>
 inline __host__ __device__ IdxT
-elem_to_loc_nd(IdxT elem, const int* shape, const int64_t* strides) {
+elem_to_loc_nd(IdxT elem, const int64_t* shape, const int64_t* strides) {
   IdxT loc = 0;
 #pragma unroll
   for (int i = NDIM - 1; i >= 0; --i) {
@@ -282,14 +282,14 @@ elem_to_loc_nd(IdxT elem, const int* shape, const int64_t* strides) {
 template <int NDIM, typename IdxT = int64_t>
 inline __host__ __device__ cuda::std::tuple<IdxT, IdxT> elem_to_loc_nd(
     IdxT elem,
-    const int* shape,
+    const int64_t* shape,
     const int64_t* a_strides,
     const int64_t* b_strides) {
   IdxT a_loc = 0;
   IdxT b_loc = 0;
 #pragma unroll
   for (int i = NDIM - 1; i >= 0; --i) {
-    int dim_idx = elem % shape[i];
+    int64_t dim_idx = elem % shape[i];
     a_loc += dim_idx * IdxT(a_strides[i]);
     b_loc += dim_idx * IdxT(b_strides[i]);
     elem /= shape[i];
@@ -300,7 +300,7 @@ inline __host__ __device__ cuda::std::tuple<IdxT, IdxT> elem_to_loc_nd(
 template <int NDIM, typename IdxT = int64_t>
 inline __host__ __device__ cuda::std::tuple<IdxT, IdxT, IdxT> elem_to_loc_nd(
     IdxT elem,
-    const int* shape,
+    const int64_t* shape,
     const int64_t* a_strides,
     const int64_t* b_strides,
     const int64_t* c_strides) {
@@ -309,7 +309,7 @@ inline __host__ __device__ cuda::std::tuple<IdxT, IdxT, IdxT> elem_to_loc_nd(
   IdxT c_loc = 0;
 #pragma unroll
   for (int i = NDIM - 1; i >= 0; --i) {
-    int dim_idx = elem % shape[i];
+    int64_t dim_idx = elem % shape[i];
     a_loc += dim_idx * IdxT(a_strides[i]);
     b_loc += dim_idx * IdxT(b_strides[i]);
     c_loc += dim_idx * IdxT(c_strides[i]);
@@ -321,14 +321,14 @@ inline __host__ __device__ cuda::std::tuple<IdxT, IdxT, IdxT> elem_to_loc_nd(
 template <typename IdxT = int64_t>
 inline __host__ __device__ cuda::std::tuple<IdxT, IdxT> elem_to_loc(
     IdxT elem,
-    const int* shape,
+    const int64_t* shape,
     const int64_t* a_strides,
     const int64_t* b_strides,
     int ndim) {
   IdxT a_loc = 0;
   IdxT b_loc = 0;
   for (int i = ndim - 1; i >= 0; --i) {
-    int dim_idx = elem % shape[i];
+    int64_t dim_idx = elem % shape[i];
     a_loc += dim_idx * IdxT(a_strides[i]);
     b_loc += dim_idx * IdxT(b_strides[i]);
     elem /= shape[i];
@@ -339,7 +339,7 @@ inline __host__ __device__ cuda::std::tuple<IdxT, IdxT> elem_to_loc(
 template <typename IdxT = int64_t>
 inline __host__ __device__ cuda::std::tuple<IdxT, IdxT, IdxT> elem_to_loc(
     IdxT elem,
-    const int* shape,
+    const int64_t* shape,
     const int64_t* a_strides,
     const int64_t* b_strides,
     const int64_t* c_strides,
@@ -348,7 +348,7 @@ inline __host__ __device__ cuda::std::tuple<IdxT, IdxT, IdxT> elem_to_loc(
   IdxT b_loc = 0;
   IdxT c_loc = 0;
   for (int i = ndim - 1; i >= 0; --i) {
-    int dim_idx = elem % shape[i];
+    int64_t dim_idx = elem % shape[i];
     a_loc += dim_idx * IdxT(a_strides[i]);
     b_loc += dim_idx * IdxT(b_strides[i]);
     c_loc += dim_idx * IdxT(c_strides[i]);
@@ -370,7 +370,7 @@ struct LoopedElemToLoc {
 
   __device__ LoopedElemToLoc(int dim) : dim(dim), inner_looper(dim - 1) {}
 
-  __device__ void next(const int* shape, const int64_t* strides) {
+  __device__ void next(const int64_t* shape, const int64_t* strides) {
     if (dim == 0) {
       return;
     }
@@ -383,7 +383,7 @@ struct LoopedElemToLoc {
     }
   }
 
-  __device__ void next(int n, const int* shape, const int64_t* strides) {
+  __device__ void next(int n, const int64_t* shape, const int64_t* strides) {
     if (dim == 0) {
       return;
     }
@@ -391,7 +391,7 @@ struct LoopedElemToLoc {
     offset += n * OffsetT(strides[dim - 1]);
 
     if (index >= shape[dim - 1]) {
-      int extra = index - shape[dim - 1];
+      int64_t extra = index - shape[dim - 1];
       if (extra >= shape[dim - 1]) {
         inner_looper.next(1 + extra / shape[dim - 1], shape, strides);
         extra = extra % shape[dim - 1];
@@ -419,7 +419,7 @@ struct LoopedElemToLoc<1, true, OffsetT> {
 
   __device__ LoopedElemToLoc(int dim) : dim(dim) {}
 
-  __device__ void next(const int* shape, const int64_t* strides) {
+  __device__ void next(const int64_t* shape, const int64_t* strides) {
     index++;
     if (dim > 1) {
       offset = elem_to_loc<OffsetT>(index, shape, strides, dim);
@@ -428,7 +428,7 @@ struct LoopedElemToLoc<1, true, OffsetT> {
     }
   }
 
-  __device__ void next(int n, const int* shape, const int64_t* strides) {
+  __device__ void next(int n, const int64_t* shape, const int64_t* strides) {
     index += n;
     if (dim > 1) {
       offset = elem_to_loc<OffsetT>(index, shape, strides, dim);
@@ -448,11 +448,11 @@ struct LoopedElemToLoc<1, false, OffsetT> {
 
   __device__ LoopedElemToLoc(int) {}
 
-  __device__ void next(const int*, const int64_t* strides) {
+  __device__ void next(const int64_t*, const int64_t* strides) {
     offset += OffsetT(strides[0]);
   }
 
-  __device__ void next(int n, const int*, const int64_t* strides) {
+  __device__ void next(int n, const int64_t*, const int64_t* strides) {
     offset += n * OffsetT(strides[0]);
   }
 
diff --git a/mlx/backend/cuda/indexing.cpp b/mlx/backend/cuda/indexing.cpp
index 8cbd443b4..f30052c27 100644
--- a/mlx/backend/cuda/indexing.cpp
+++ b/mlx/backend/cuda/indexing.cpp
@@ -39,7 +39,7 @@ void append_indices_arg(
     indices[i] = gpu_ptr<void>(inputs[i + 1]);
   }
   args.append(std::move(indices));
-  SmallVector<int32_t> indices_shape(nidx * idx_ndim);
+  SmallVector<int64_t> indices_shape(nidx * idx_ndim);
   for (int i = 0; i < nidx; ++i) {
     std::copy_n(
         inputs[i + 1].shape().begin(),
@@ -167,11 +167,11 @@ void Scatter::eval_gpu(const std::vector<array>& inputs, array& out) {
   bool large = (nidx > 0 && inputs[1].size() > INT32_MAX) ||
       (upd.size() > INT32_MAX) || (out.size() > INT32_MAX);
 
-  int32_t upd_post_idx_size = std::accumulate(
+  int64_t upd_post_idx_size = std::accumulate(
       upd.shape().begin() + idx_ndim,
       upd.shape().end(),
-      1,
-      std::multiplies<int32_t>());
+      int64_t(1),
+      std::multiplies<int64_t>());
 
   const char* op = g_scatter_ops[reduce_type_];
   std::string module_name = fmt::format(
diff --git a/mlx/backend/cuda/kernel_utils.cuh b/mlx/backend/cuda/kernel_utils.cuh
index 02868961f..6b308df15 100644
--- a/mlx/backend/cuda/kernel_utils.cuh
+++ b/mlx/backend/cuda/kernel_utils.cuh
@@ -102,7 +102,7 @@ template <typename T>
 inline constexpr bool is_inexact_v = is_floating_v<T> || is_complex_v<T>;
 
 // Utility to copy data from vector to array in host.
-template <int NDIM = MAX_NDIM, typename T = int32_t>
+template <int NDIM = MAX_NDIM, typename T = int64_t>
 inline cuda::std::array<T, NDIM> const_param(const SmallVector<T>& vec) {
   if (vec.size() > NDIM) {
     throw std::runtime_error(
diff --git a/mlx/backend/metal/compiled.cpp b/mlx/backend/metal/compiled.cpp
index eb51ab750..d30dea6fa 100644
--- a/mlx/backend/metal/compiled.cpp
+++ b/mlx/backend/metal/compiled.cpp
@@ -71,7 +71,7 @@ inline void build_kernel(
   // Add output strides and shape to extract the indices.
   if (!contiguous) {
     os += fmt::format(
-        "    constant const int* output_shape [[buffer({0})]],\n", cnt++);
+        "    constant const int64_t* output_shape [[buffer({0})]],\n", cnt++);
   } else {
     os += fmt::format(
         "    constant const {0}& size [[buffer({1})]],\n", idx_type, cnt++);
@@ -93,7 +93,7 @@ inline void build_kernel(
     os += "  uint index = N_ * pos.x;\n";
   } else if (work_per_thread > 1) {
     os += fmt::format(
-        "  int xshape = output_shape[{0}];\n",
+        "  int64_t xshape = output_shape[{0}];\n",
         dynamic_dims ? "ndim - 1" : std::to_string(ndim - 1));
     os += fmt::format(
         "  {0} index = N_ * pos.x + xshape * (pos.y + {0}(grid.y) * pos.z);\n",
@@ -173,7 +173,7 @@ inline void build_kernel(
     } else {
       os += fmt::format("  for (int d = {0}; d >= 0; --d) {{\n", ndim - 3);
     }
-    os += "    uint l = zpos % output_shape[d];\n";
+    os += "    int64_t l = int64_t(zpos) % output_shape[d];\n";
     for (int i = 0; i < nc_inputs.size(); ++i) {
       auto& xname = namer.get_name(nc_inputs[i]);
       os += fmt::format("    index_{0} += ", xname);
@@ -185,13 +185,13 @@ inline void build_kernel(
             fmt::format("l * {0}(in_strides[{1} + d]);\n", idx_type, i * ndim);
       }
     }
-    os += "    zpos /= output_shape[d];\n  }\n";
+    os += "    zpos /= uint(output_shape[d]);\n  }\n";
   }
 
   // Open per-thread loop
   if (work_per_thread > 1 && !contiguous) {
     os +=
-        "  for (int i = 0; i < N_ && (int(N_ * pos.x) + i) < xshape; ++i) {\n";
+        "  for (int i = 0; i < N_ && (int64_t(N_ * pos.x) + i) < xshape; ++i) {\n";
   }
 
   // Read non-contiguous inputs into tmps
diff --git a/mlx/backend/metal/conv.cpp b/mlx/backend/metal/conv.cpp
index fac135fe1..d295dd510 100644
--- a/mlx/backend/metal/conv.cpp
+++ b/mlx/backend/metal/conv.cpp
@@ -39,11 +39,12 @@ void explicit_gemm_conv_ND_gpu(
     array& out,
     const MLXConvParams<N>& conv_params) {
   // Get gemm shapes
-  int implicit_M = out.size() / conv_params.O;
-  int implicit_K = wt.size() / conv_params.O;
+  size_t implicit_M = out.size() / conv_params.O;
+  size_t implicit_K = wt.size() / conv_params.O;
   int implicit_N = conv_params.O;
   // Prepare unfolding array
-  Shape unfolded_shape{implicit_M, implicit_K};
+  Shape unfolded_shape{
+      static_cast<ShapeElem>(implicit_M), static_cast<ShapeElem>(implicit_K)};
   array in_unfolded(unfolded_shape, in.dtype(), nullptr, {});
 
   in_unfolded.set_data(allocator::malloc(in_unfolded.nbytes()));
@@ -74,8 +75,8 @@ void explicit_gemm_conv_ND_gpu(
   compute_encoder.dispatch_threads(grid_dims, group_dims);
 
   // Reshape weight
-  Shape wt_reshape{implicit_K, implicit_N};
-  Strides wt_restride{1, implicit_K};
+  Shape wt_reshape{static_cast<ShapeElem>(implicit_K), static_cast<ShapeElem>(implicit_N)};
+  Strides wt_restride{1, static_cast<int64_t>(implicit_K)};
   array wt_reshaped(wt_reshape, wt.dtype(), nullptr, {});
   auto wt_flags = wt.flags();
   wt_flags.row_contiguous = false;
@@ -113,8 +114,8 @@ void explicit_gemm_conv_group_ND_gpu(
   const int C_per_group = conv_params.C / conv_params.groups;
   const int O_per_group = conv_params.O / conv_params.groups;
   // Get gemm shapes
-  const int implicit_M = out.size() / conv_params.O;
-  const int implicit_K = wt.size() / conv_params.O;
+  const size_t implicit_M = out.size() / conv_params.O;
+  const size_t implicit_K = wt.size() / conv_params.O;
   const int implicit_N = O_per_group;
 
   int kernel_size = 1;
@@ -123,7 +124,9 @@ void explicit_gemm_conv_group_ND_gpu(
   }
 
   // Prepare unfolding array
-  Shape unfolded_shape{implicit_M, implicit_K * groups};
+  Shape unfolded_shape{
+      static_cast<ShapeElem>(implicit_M),
+      static_cast<ShapeElem>(implicit_K * groups)};
   array in_unfolded(unfolded_shape, in.dtype(), nullptr, {});
   in_unfolded.set_data(allocator::malloc(in_unfolded.nbytes()));
 
@@ -171,12 +174,12 @@ void explicit_gemm_conv_group_ND_gpu(
       /* const array& a = */ in_unfolded,
       /* const array& b = */ wt_transpose,
       /* array& c = */ out,
-      /* int M = */ implicit_M,
+      /* int M = */ static_cast<int>(implicit_M),
       /* int N = */ implicit_N,
-      /* int K = */ implicit_K,
+      /* int K = */ static_cast<int>(implicit_K),
       /* int batch_size_out = */ groups,
-      /* int lda = */ implicit_K * groups,
-      /* int ldb = */ implicit_K,
+      /* int lda = */ static_cast<int>(implicit_K * groups),
+      /* int ldb = */ static_cast<int>(implicit_K),
       /* int ldd = */ implicit_N * groups,
       /* bool transpose_a = */ false,
       /* bool transpose_b = */ true,
@@ -200,7 +203,8 @@ void implicit_gemm_conv_2D_gpu(
   const int O_per_group = conv_params.O / conv_params.groups;
 
   // Deduce implicit gemm size
-  const int implicit_M = conv_params.N * conv_params.oS[0] * conv_params.oS[1];
+  const int64_t implicit_M =
+      int64_t(conv_params.N) * conv_params.oS[0] * conv_params.oS[1];
   const int implicit_N = O_per_group;
   const int implicit_K = conv_params.wS[0] * conv_params.wS[1] * C_per_group;
 
@@ -218,7 +222,7 @@ void implicit_gemm_conv_2D_gpu(
   }
 
   int tn = (implicit_N + bn - 1) / bn;
-  int tm = (implicit_M + bm - 1) / bm;
+  int tm = static_cast<int>((implicit_M + bm - 1) / bm);
   int swizzle_log = 0;
 
   // Fix small channel specialization
@@ -329,7 +333,8 @@ void implicit_gemm_conv_2D_general_gpu(
     array& out,
     const MLXConvParams<2>& conv_params) {
   // Deduce implicit gemm size
-  int implicit_M = conv_params.N * conv_params.oS[0] * conv_params.oS[1];
+  int64_t implicit_M =
+      int64_t(conv_params.N) * conv_params.oS[0] * conv_params.oS[1];
   int implicit_N = conv_params.O;
   int implicit_K = conv_params.wS[0] * conv_params.wS[1] * conv_params.C;
 
@@ -350,7 +355,7 @@ void implicit_gemm_conv_2D_general_gpu(
   int adj_out_h = (conv_params.oS[0] + f_out_jump_h - 1) / f_out_jump_h;
   int adj_out_w = (conv_params.oS[1] + f_out_jump_w - 1) / f_out_jump_w;
   int adj_out_hw = adj_out_h * adj_out_w;
-  int adj_implicit_m = conv_params.N * adj_out_hw;
+  int64_t adj_implicit_m = int64_t(conv_params.N) * adj_out_hw;
 
   Conv2DGeneralJumpParams jump_params{
       /* const int f_wgt_jump_h = */ f_wgt_jump_h,
@@ -410,7 +415,7 @@ void implicit_gemm_conv_2D_general_gpu(
   int bk = 16;
 
   int tn = (implicit_N + bn - 1) / bn;
-  int tm = (adj_implicit_m + bm - 1) / bm;
+  int tm = static_cast<int>((adj_implicit_m + bm - 1) / bm);
   int swizzle_log = 0;
 
   // Get channel iteration info
@@ -512,8 +517,8 @@ void implicit_gemm_conv_3D_gpu(
   const int O_per_group = conv_params.O / conv_params.groups;
 
   // Deduce implicit gemm size
-  const int implicit_M =
-      conv_params.N * conv_params.oS[0] * conv_params.oS[1] * conv_params.oS[2];
+  const int64_t implicit_M = int64_t(conv_params.N) * conv_params.oS[0] *
+      conv_params.oS[1] * conv_params.oS[2];
   const int implicit_N = O_per_group;
   const int implicit_K =
       conv_params.wS[0] * conv_params.wS[1] * conv_params.wS[2] * C_per_group;
@@ -532,7 +537,7 @@ void implicit_gemm_conv_3D_gpu(
   }
 
   int tn = (implicit_N + bn - 1) / bn;
-  int tm = (implicit_M + bm - 1) / bm;
+  int tm = static_cast<int>((implicit_M + bm - 1) / bm);
   int swizzle_log = 0;
 
   bool small_filter =
@@ -1002,10 +1007,10 @@ void dispatch_conv_2D_gpu(
 
   // Direct to winograd conv
   bool inp_large =
-      (conv_params.N * conv_params.iS[0] * conv_params.iS[1]) >= 4096;
+      (int64_t(conv_params.N) * conv_params.iS[0] * conv_params.iS[1]) >= 4096;
   bool channels_large = (conv_params.C + conv_params.O) >= 256;
   bool out_large =
-      (conv_params.N * conv_params.oS[0] * conv_params.oS[1]) >= 256;
+      (int64_t(conv_params.N) * conv_params.oS[0] * conv_params.oS[1]) >= 256;
   if (!conv_params.flip && is_stride_one && is_kdil_one && is_idil_one &&
       conv_params.wS[0] == 3 && conv_params.wS[1] == 3 &&
       conv_params.C % 32 == 0 && conv_params.O % 32 == 0 && inp_large &&
diff --git a/mlx/backend/metal/kernels/binary.h b/mlx/backend/metal/kernels/binary.h
index f1df88535..73c6a5c77 100644
--- a/mlx/backend/metal/kernels/binary.h
+++ b/mlx/backend/metal/kernels/binary.h
@@ -179,7 +179,7 @@ template <
     device const T* a,
     device const T* b,
     device U* c,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     constant const int& ndim,
@@ -191,7 +191,7 @@ template <
   IdxT out_idx = N * index.x + xshape * (index.y + IdxT(grid_dim.y) * index.z);
   IdxT a_xstride = a_strides[ndim - 1];
   IdxT b_xstride = b_strides[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     c[out_idx++] = Op()(a[idx.x], b[idx.y]);
     idx.x += a_xstride;
     idx.y += b_xstride;
diff --git a/mlx/backend/metal/kernels/binary_two.h b/mlx/backend/metal/kernels/binary_two.h
index 4455e4ca9..c97a05f0c 100644
--- a/mlx/backend/metal/kernels/binary_two.h
+++ b/mlx/backend/metal/kernels/binary_two.h
@@ -222,7 +222,7 @@ template <
     device const T* b,
     device U* c,
     device U* d,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     constant const int& ndim,
@@ -234,7 +234,7 @@ template <
   IdxT out_idx = N * index.x + xshape * (index.y + IdxT(grid_dim.y) * index.z);
   IdxT a_xstride = a_strides[ndim - 1];
   IdxT b_xstride = b_strides[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     auto out = Op()(a[idx.x], b[idx.y]);
     c[out_idx] = out[0];
     d[out_idx++] = out[1];
diff --git a/mlx/backend/metal/kernels/copy.h b/mlx/backend/metal/kernels/copy.h
index cf22347ee..762796fdf 100644
--- a/mlx/backend/metal/kernels/copy.h
+++ b/mlx/backend/metal/kernels/copy.h
@@ -113,7 +113,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
 [[kernel]] void copy_g(
     device const T* src [[buffer(0)]],
     device U* dst [[buffer(1)]],
-    constant const int* src_shape [[buffer(2)]],
+    constant const int64_t* src_shape [[buffer(2)]],
     constant const int64_t* src_strides [[buffer(3)]],
     constant const int& ndim [[buffer(5)]],
     uint3 index [[thread_position_in_grid]],
@@ -129,7 +129,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
   auto xshape = src_shape[ndim - 1];
   IdxT dst_idx = N * index.x + xshape * (index.y + IdxT(grid_dim.y) * index.z);
   auto src_xstride = src_strides[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     dst[dst_idx + i] = static_cast<U>(src[src_idx]);
     src_idx += src_xstride;
   }
@@ -175,7 +175,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
 [[kernel]] void copy_gg(
     device const T* src [[buffer(0)]],
     device U* dst [[buffer(1)]],
-    constant const int* src_shape [[buffer(2)]],
+    constant const int64_t* src_shape [[buffer(2)]],
     constant const int64_t* src_strides [[buffer(3)]],
     constant const int64_t* dst_strides [[buffer(4)]],
     constant const int& ndim [[buffer(5)]],
@@ -193,7 +193,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
   IdxT src_xstride = src_strides[ndim - 1];
   IdxT dst_xstride = dst_strides[ndim - 1];
   auto xshape = src_shape[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     dst[idx.y] = static_cast<U>(src[idx.x]);
     idx.x += src_xstride;
     idx.y += dst_xstride;
@@ -246,7 +246,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
 [[kernel]] void copy_gg_dynamic(
     device const T* src [[buffer(0)]],
     device U* dst [[buffer(1)]],
-    constant const int* src_shape [[buffer(2)]],
+    constant const int64_t* src_shape [[buffer(2)]],
     constant const int64_t* src_strides [[buffer(3)]],
     constant const int64_t* dst_strides [[buffer(4)]],
     constant const int& ndim [[buffer(5)]],
@@ -268,7 +268,7 @@ template <typename T, typename U, int N = 1, typename IdxT = int64_t>
   IdxT src_xstride = src_strides[ndim - 1];
   IdxT dst_xstride = dst_strides[ndim - 1];
   auto xshape = src_shape[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     dst[idx.y] = src[idx.x];
     idx.x += src_xstride;
     idx.y += dst_xstride;
diff --git a/mlx/backend/metal/kernels/indexing/gather.h b/mlx/backend/metal/kernels/indexing/gather.h
index 8b93c0167..32a34c583 100644
--- a/mlx/backend/metal/kernels/indexing/gather.h
+++ b/mlx/backend/metal/kernels/indexing/gather.h
@@ -8,10 +8,10 @@ template <typename T, typename IdxT, int NIDX, int IDX_NDIM, typename LocT>
 METAL_FUNC void gather_impl(
     const device T* src [[buffer(0)]],
     device T* out [[buffer(1)]],
-    const constant int* src_shape [[buffer(2)]],
+    const constant int64_t* src_shape [[buffer(2)]],
     const constant int64_t* src_strides [[buffer(3)]],
     const constant size_t& src_ndim [[buffer(4)]],
-    const constant int* slice_sizes [[buffer(5)]],
+    const constant int64_t* slice_sizes [[buffer(5)]],
     const constant int* axes [[buffer(6)]],
     const thread Indices<IdxT, NIDX>& indices,
     uint3 index [[thread_position_in_grid]],
diff --git a/mlx/backend/metal/kernels/indexing/masked_scatter.h b/mlx/backend/metal/kernels/indexing/masked_scatter.h
index 2ba547400..e33c7fadd 100644
--- a/mlx/backend/metal/kernels/indexing/masked_scatter.h
+++ b/mlx/backend/metal/kernels/indexing/masked_scatter.h
@@ -10,7 +10,7 @@ template <typename T, bool src_contiguous>
     const device uint* scatter_offsets [[buffer(1)]],
     const device T* src [[buffer(2)]],
     device T* out [[buffer(3)]],
-    const constant int* src_shapes [[buffer(4)]],
+    const constant int64_t* src_shapes [[buffer(4)]],
     const constant int64_t* src_strides [[buffer(5)]],
     const constant int& src_ndim [[buffer(6)]],
     const constant int64_t& src_batch_size [[buffer(7)]],
diff --git a/mlx/backend/metal/kernels/random.metal b/mlx/backend/metal/kernels/random.metal
index ccbd464d3..7464bb13a 100644
--- a/mlx/backend/metal/kernels/random.metal
+++ b/mlx/backend/metal/kernels/random.metal
@@ -70,7 +70,7 @@ rbits threefry2x32_hash(const thread uint2& key, uint2 count) {
     constant const bool& odd,
     constant const uint& bytes_per_key,
     constant const int& ndim,
-    constant const int* key_shape,
+    constant const int64_t* key_shape,
     constant const int64_t* key_strides,
     uint2 grid_dim [[threads_per_grid]],
     uint2 index [[thread_position_in_grid]]) {
diff --git a/mlx/backend/metal/kernels/reduction/reduce_col.h b/mlx/backend/metal/kernels/reduction/reduce_col.h
index c109faf0b..0dd2585f1 100644
--- a/mlx/backend/metal/kernels/reduction/reduce_col.h
+++ b/mlx/backend/metal/kernels/reduction/reduce_col.h
@@ -6,10 +6,10 @@ template <typename T, typename U, typename Op, typename IdxT, int NDIMS>
     device U* out [[buffer(1)]],
     const constant size_t& reduction_size [[buffer(2)]],
     const constant int64_t& reduction_stride [[buffer(3)]],
-    const constant int* shape [[buffer(4)]],
+    const constant int64_t* shape [[buffer(4)]],
     const constant int64_t* strides [[buffer(5)]],
     const constant int& ndim [[buffer(6)]],
-    const constant int* reduce_shape [[buffer(7)]],
+    const constant int64_t* reduce_shape [[buffer(7)]],
     const constant int64_t* reduce_strides [[buffer(8)]],
     const constant int& reduce_ndim [[buffer(9)]],
     const constant size_t& non_col_reductions [[buffer(10)]],
@@ -99,10 +99,10 @@ template <typename T, typename U, typename Op, typename IdxT, int NDIMS>
     device U* out [[buffer(1)]],
     const constant size_t& reduction_size [[buffer(2)]],
     const constant size_t& reduction_stride [[buffer(3)]],
-    const constant int* shape [[buffer(4)]],
+    const constant int64_t* shape [[buffer(4)]],
     const constant int64_t* strides [[buffer(5)]],
     const constant int& ndim [[buffer(6)]],
-    const constant int* reduce_shape [[buffer(7)]],
+    const constant int64_t* reduce_shape [[buffer(7)]],
     const constant int64_t* reduce_strides [[buffer(8)]],
     const constant int& reduce_ndim [[buffer(9)]],
     const constant size_t& non_col_reductions [[buffer(10)]],
@@ -165,10 +165,10 @@ template <
     device U* out [[buffer(1)]],
     const constant size_t& reduction_size [[buffer(2)]],
     const constant int64_t& reduction_stride [[buffer(3)]],
-    const constant int* shape [[buffer(4)]],
+    const constant int64_t* shape [[buffer(4)]],
     const constant int64_t* strides [[buffer(5)]],
     const constant int& ndim [[buffer(6)]],
-    const constant int* reduce_shape [[buffer(7)]],
+    const constant int64_t* reduce_shape [[buffer(7)]],
     const constant int64_t* reduce_strides [[buffer(8)]],
     const constant int& reduce_ndim [[buffer(9)]],
     const constant size_t& non_col_reductions [[buffer(10)]],
@@ -304,10 +304,10 @@ template <
     device U* out [[buffer(1)]],
     const constant size_t& reduction_size [[buffer(2)]],
     const constant int64_t& reduction_stride [[buffer(3)]],
-    const constant int* shape [[buffer(4)]],
+    const constant int64_t* shape [[buffer(4)]],
     const constant int64_t* strides [[buffer(5)]],
     const constant int& ndim [[buffer(6)]],
-    const constant int* reduce_shape [[buffer(7)]],
+    const constant int64_t* reduce_shape [[buffer(7)]],
     const constant int64_t* reduce_strides [[buffer(8)]],
     const constant int& reduce_ndim [[buffer(9)]],
     const constant size_t& non_col_reductions [[buffer(10)]],
diff --git a/mlx/backend/metal/kernels/reduction/reduce_row.h b/mlx/backend/metal/kernels/reduction/reduce_row.h
index 936d75bb5..a6c9766f7 100644
--- a/mlx/backend/metal/kernels/reduction/reduce_row.h
+++ b/mlx/backend/metal/kernels/reduction/reduce_row.h
@@ -101,7 +101,7 @@ METAL_FUNC void per_thread_row_reduce(
     const int64_t row_idx,
     int blocks,
     int extra,
-    const constant int* shape,
+    const constant int64_t* shape,
     const constant int64_t* strides,
     const constant int& ndim,
     uint lsize_x,
diff --git a/mlx/backend/metal/kernels/steel/conv/params.h b/mlx/backend/metal/kernels/steel/conv/params.h
index 67d382744..87e202f81 100644
--- a/mlx/backend/metal/kernels/steel/conv/params.h
+++ b/mlx/backend/metal/kernels/steel/conv/params.h
@@ -49,7 +49,7 @@ namespace mlx {
 namespace steel {
 
 struct ImplicitGemmConv2DParams {
-  const int M;
+  const int64_t M;
   const int N;
   const int K;
 
@@ -65,7 +65,7 @@ struct ImplicitGemmConv2DParams {
 };
 
 struct ImplicitGemmConv3DParams {
-  const int M;
+  const int64_t M;
   const int N;
   const int K;
 
@@ -91,7 +91,7 @@ struct Conv2DGeneralJumpParams {
   const int adj_out_h;
   const int adj_out_w;
   const int adj_out_hw;
-  const int adj_implicit_m;
+  const int64_t adj_implicit_m;
 };
 
 struct Conv2DGeneralBaseInfo {
diff --git a/mlx/backend/metal/kernels/steel/utils.h b/mlx/backend/metal/kernels/steel/utils.h
index 55720a28f..ffafa381d 100644
--- a/mlx/backend/metal/kernels/steel/utils.h
+++ b/mlx/backend/metal/kernels/steel/utils.h
@@ -6,15 +6,15 @@
 
 METAL_FUNC ulong2 elem_to_loc_broadcast(
     uint elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     int ndim) {
   ulong loc_a{0};
   ulong loc_b{0};
   for (int i = ndim - 1; i >= 0 && elem > 0; --i) {
-    int pos_in_dim = (elem % shape[i]);
-    elem /= shape[i];
+    int64_t pos_in_dim = (int64_t(elem) % shape[i]);
+    elem /= uint(shape[i]);
     loc_a += pos_in_dim * a_strides[i];
     loc_b += pos_in_dim * b_strides[i];
   }
@@ -23,7 +23,7 @@ METAL_FUNC ulong2 elem_to_loc_broadcast(
 
 METAL_FUNC ulong3 elem_to_loc_broadcast(
     uint elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     constant const int64_t* c_strides,
@@ -32,8 +32,8 @@ METAL_FUNC ulong3 elem_to_loc_broadcast(
   ulong loc_b{0};
   ulong loc_c{0};
   for (int i = ndim - 1; i >= 0 && elem > 0; --i) {
-    int pos_in_dim = (elem % shape[i]);
-    elem /= shape[i];
+    int64_t pos_in_dim = (int64_t(elem) % shape[i]);
+    elem /= uint(shape[i]);
     loc_a += pos_in_dim * a_strides[i];
     loc_b += pos_in_dim * b_strides[i];
     loc_c += pos_in_dim * c_strides[i];
diff --git a/mlx/backend/metal/kernels/ternary.h b/mlx/backend/metal/kernels/ternary.h
index 705b73e25..f5949dbae 100644
--- a/mlx/backend/metal/kernels/ternary.h
+++ b/mlx/backend/metal/kernels/ternary.h
@@ -117,7 +117,7 @@ template <typename T, typename Op, int N = 1, typename IdxT = int64_t>
     device const T* b,
     device const T* c,
     device T* d,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     constant const int64_t* c_strides,
@@ -136,7 +136,7 @@ template <typename T, typename Op, int N = 1, typename IdxT = int64_t>
   IdxT a_xstride = a_strides[ndim - 1];
   IdxT b_xstride = b_strides[ndim - 1];
   IdxT c_xstride = c_strides[ndim - 1];
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     d[out_idx++] = Op()(a[idx.x], b[idx.y], c[idx.z]);
     idx.x += a_xstride;
     idx.y += b_xstride;
diff --git a/mlx/backend/metal/kernels/unary.h b/mlx/backend/metal/kernels/unary.h
index db7be3d41..7b60e655f 100644
--- a/mlx/backend/metal/kernels/unary.h
+++ b/mlx/backend/metal/kernels/unary.h
@@ -46,7 +46,7 @@ template <
 [[kernel]] void unary_g(
     device const T* in,
     device U* out,
-    constant const int* in_shape,
+    constant const int64_t* in_shape,
     constant const int64_t* in_strides,
     device const int& ndim,
     uint3 index [[thread_position_in_grid]],
@@ -56,7 +56,7 @@ template <
   auto xshape = in_shape[ndim - 1];
   IdxT xstride = in_strides[ndim - 1];
   IdxT out_idx = N * index.x + xshape * (index.y + IdxT(grid_dim.y) * index.z);
-  for (int i = 0; i < N && (int(N * index.x) + i) < xshape; ++i) {
+  for (int i = 0; i < N && (int64_t(N * index.x) + i) < xshape; ++i) {
     out[out_idx++] = static_cast<U>(Op()(in[idx]));
     idx += xstride;
   }
diff --git a/mlx/backend/metal/kernels/utils.h b/mlx/backend/metal/kernels/utils.h
index d35645010..34b3103c0 100644
--- a/mlx/backend/metal/kernels/utils.h
+++ b/mlx/backend/metal/kernels/utils.h
@@ -97,7 +97,7 @@ struct Limits<complex64_t> {
 template <typename IdxT = int64_t>
 METAL_FUNC IdxT elem_to_loc(
     IdxT elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* strides,
     int ndim) {
   IdxT loc = 0;
@@ -112,14 +112,14 @@ METAL_FUNC IdxT elem_to_loc(
 template <typename IdxT = int64_t>
 METAL_FUNC IdxT elem_to_loc(
     uint3 elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* strides,
     int ndim) {
   IdxT loc =
       elem.x * IdxT(strides[ndim - 1]) + elem.y * IdxT(strides[ndim - 2]);
   for (int d = ndim - 3; d >= 0; --d) {
-    loc += (elem.z % shape[d]) * IdxT(strides[d]);
-    elem.z /= shape[d];
+    loc += (int64_t(elem.z) % shape[d]) * IdxT(strides[d]);
+    elem.z /= uint(shape[d]);
   }
   return loc;
 }
@@ -149,7 +149,7 @@ METAL_FUNC IdxT elem_to_loc_3(uint3 elem, constant const int64_t strides[3]) {
 template <typename IdxT = int64_t>
 METAL_FUNC vec<IdxT, 2> elem_to_loc_2_nd(
     uint3 elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     int ndim) {
@@ -161,10 +161,10 @@ METAL_FUNC vec<IdxT, 2> elem_to_loc_2_nd(
           elem.x * IdxT(b_strides[ndim - 1]) +
           elem.y * IdxT(b_strides[ndim - 2]))};
   for (int d = ndim - 3; d >= 0; --d) {
-    uint l = elem.z % shape[d];
+    int64_t l = int64_t(elem.z) % shape[d];
     loc.x += l * IdxT(a_strides[d]);
     loc.y += l * IdxT(b_strides[d]);
-    elem.z /= shape[d];
+    elem.z /= uint(shape[d]);
   }
   return loc;
 }
@@ -172,7 +172,7 @@ METAL_FUNC vec<IdxT, 2> elem_to_loc_2_nd(
 template <typename IdxT = int64_t>
 METAL_FUNC vec<IdxT, 3> elem_to_loc_3_nd(
     uint3 elem,
-    constant const int* shape,
+    constant const int64_t* shape,
     constant const int64_t* a_strides,
     constant const int64_t* b_strides,
     constant const int64_t* c_strides,
@@ -185,11 +185,11 @@ METAL_FUNC vec<IdxT, 3> elem_to_loc_3_nd(
       IdxT(elem.x * IdxT(c_strides[ndim - 1])) +
           IdxT(elem.y * IdxT(c_strides[ndim - 2]))};
   for (int d = ndim - 3; d >= 0; --d) {
-    uint l = elem.z % shape[d];
+    int64_t l = int64_t(elem.z) % shape[d];
     loc.x += l * IdxT(a_strides[d]);
     loc.y += l * IdxT(b_strides[d]);
     loc.z += l * IdxT(c_strides[d]);
-    elem.z /= shape[d];
+    elem.z /= uint(shape[d]);
   }
   return loc;
 }
@@ -207,7 +207,7 @@ struct LoopedElemToLoc {
 
   LoopedElemToLoc(int dim) : dim(dim), inner_looper(dim - 1) {}
 
-  void next(const constant int* shape, const constant int64_t* strides) {
+  void next(const constant int64_t* shape, const constant int64_t* strides) {
     if (dim == 0) {
       return;
     }
@@ -220,7 +220,7 @@ struct LoopedElemToLoc {
     }
   }
 
-  void next(int n, const constant int* shape, const constant int64_t* strides) {
+  void next(int n, const constant int64_t* shape, const constant int64_t* strides) {
     if (dim == 0) {
       return;
     }
@@ -256,7 +256,7 @@ struct LoopedElemToLoc<1, OffsetT, true> {
 
   LoopedElemToLoc(int dim) : dim(dim) {}
 
-  void next(const constant int* shape, const constant int64_t* strides) {
+  void next(const constant int64_t* shape, const constant int64_t* strides) {
     index++;
     if (dim > 1) {
       offset = elem_to_loc<OffsetT>(index, shape, strides, dim);
@@ -265,7 +265,7 @@ struct LoopedElemToLoc<1, OffsetT, true> {
     }
   }
 
-  void next(int n, const constant int* shape, const constant int64_t* strides) {
+  void next(int n, const constant int64_t* shape, const constant int64_t* strides) {
     index += n;
     if (dim > 1) {
       offset = elem_to_loc<OffsetT>(index, shape, strides, dim);
@@ -285,11 +285,11 @@ struct LoopedElemToLoc<1, OffsetT, false> {
 
   LoopedElemToLoc(int) {}
 
-  void next(const constant int*, const constant int64_t* strides) {
+  void next(const constant int64_t*, const constant int64_t* strides) {
     offset += OffsetT(strides[0]);
   }
 
-  void next(int n, const constant int*, const constant int64_t* strides) {
+  void next(int n, const constant int64_t*, const constant int64_t* strides) {
     offset += n * OffsetT(strides[0]);
   }
 
diff --git a/python/src/convert.cpp b/python/src/convert.cpp
index e004ac9cf..33ce4bae9 100644
--- a/python/src/convert.cpp
+++ b/python/src/convert.cpp
@@ -25,12 +25,12 @@ struct ndarray_traits<mx::float16_t> {
 };
 }; // namespace nanobind
 
-int check_shape_dim(int64_t dim) {
-  if (dim > std::numeric_limits<int>::max()) {
+mx::ShapeElem check_shape_dim(int64_t dim) {
+  if (dim > std::numeric_limits<mx::ShapeElem>::max()) {
     throw std::invalid_argument(
-        "Shape dimension falls outside supported `int` range.");
+        "Shape dimension falls outside supported range.");
   }
-  return static_cast<int>(dim);
+  return static_cast<mx::ShapeElem>(dim);
 }
 
 template <typename T>
